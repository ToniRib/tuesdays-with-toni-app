<!DOCTYPE html>
<html>
  <head>
    <title>TuesdaysWithToni</title>
    <%= csrf_meta_tags %>

    <link href="https://fonts.googleapis.com/css?family=Muli" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
  <body>
    <div class="row site-title-row">
      <div class="col-sm-12 text-center" id="main-title">
        <img src="<%= image_path "tuesdays-with-toni-logo-colored.svg" %>" class="site-title" />
      </div>
    </div>

    <%= render partial: 'layouts/navbar' %>

    <% flash.each do |type, message| %>
      <%= content_tag :div, sanitize(message), class: "flash_#{type}" %>
    <% end %>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
